<% content_for (:html_title) { @type.name } %>

<div class="content">
  <h1><%= @type.name %></h1>

  <table class="table">
    <%= render 'places/header' %>
    <tbody>
    <%= render @type.places %>
    </tbody>
  </table>
</div>